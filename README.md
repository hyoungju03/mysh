# mysh - a very basic C shell

mysh is my attempt to recreate a Unix shell in C. To try it, clone the repo:
`git clone https://github.com/hyoungju03/http_server_c.git`

In the project directory, build and run with:
`make && ./bin/mysh`
